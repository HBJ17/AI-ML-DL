import numpy as np

# Create array
arr = np.array([1, 2, 3, 4, 5])
print("Array:", arr)

# Array operations
print("Mean:", np.mean(arr))
print("Sum:", np.sum(arr))
print("Square Root:", np.sqrt(arr))
